# TLV Bot frontend

React + TypeScript, built with Vite straight into `../static` (see
`vite.config.ts`) for FastAPI to serve. See the repo root `README.md` for full
setup instructions.

```bash
npm install
npm run dev     # local dev server on :5173, proxies /api to :8000
npm run build   # builds into ../static
```
